import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.idarahwaliulaser.maktabwaliulaser',
  appName: 'Idarahwaliulaser',
  webDir: 'dist'
};

export default config;
